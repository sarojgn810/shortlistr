requests>=2.31.0
httpx>=0.27.0
openpyxl>=3.1.2
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
playwright>=1.40.0
PyYAML>=6.0
pytest>=8.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
pypdf>=5.0.0
# Preferred PDF reader: keeps letter-spaced résumés readable (pypdf does not).
pdfplumber>=0.11.0
python-docx>=1.1.0
keyring>=25.0.0
mcp>=1.0.0

# Cloud LLM SDKs — bundled so a pasted API key produces real evaluations out of the
# box (no silent fallback to keyword mode). Ollama needs none (uses requests, above).
openai>=1.0
anthropic>=0.39
google-genai>=1.0
pytesseract>=0.3.10  # OCR for referrer-shared job screenshots (needs the tesseract binary)
pillow>=10.0.0
